Q1: Kruskal’s Algorithm (Minimum Spanning Tree)
#include <stdio.h>

#define MAX 10

int parent[MAX];

// Find function
int find(int i) {
    while(parent[i])
        i = parent[i];
    return i;
}

// Union function
void unionSet(int i, int j) {
    parent[j] = i;
}

int main() {
    int n, cost[MAX][MAX];
    int i, j, min, a, b, u, v;
    int total_cost = 0, edges = 1;

    printf("Enter number of vertices: ");
    scanf("%d", &n);

    printf("Enter cost adjacency matrix:\n");
    for(i = 1; i <= n; i++) {
        for(j = 1; j <= n; j++) {
            scanf("%d", &cost[i][j]);
            if(cost[i][j] == 0)
                cost[i][j] = 999; // no edge
        }
    }

    while(edges < n) {
        min = 999;
        for(i = 1; i <= n; i++) {
            for(j = 1; j <= n; j++) {
                if(cost[i][j] < min) {
                    min = cost[i][j];
                    a = u = i;
                    b = v = j;
                }
            }
        }

        u = find(u);
        v = find(v);

        if(u != v) {
            printf("Edge %d: (%d -> %d) = %d\n", edges++, a, b, min);
            total_cost += min;
            unionSet(u, v);
        }

        cost[a][b] = cost[b][a] = 999;
    }

    printf("Minimum Cost = %d\n", total_cost);

    return 0;
}
Q2: Huffman Coding (Greedy Method)
#include <stdio.h>
#include <stdlib.h>

struct Node {
    char data;
    int freq;
    struct Node *left, *right;
};

// Create new node
struct Node* newNode(char data, int freq) {
    struct Node* temp = (struct Node*)malloc(sizeof(struct Node));
    temp->data = data;
    temp->freq = freq;
    temp->left = temp->right = NULL;
    return temp;
}

// Print codes
void printCodes(struct Node* root, int arr[], int top) {
    if(root->left) {
        arr[top] = 0;
        printCodes(root->left, arr, top + 1);
    }

    if(root->right) {
        arr[top] = 1;
        printCodes(root->right, arr, top + 1);
    }

    if(!root->left && !root->right) {
        printf("%c: ", root->data);
        for(int i = 0; i < top; i++)
            printf("%d", arr[i]);
        printf("\n");
    }
}

// Simple build (manual for exam)
int main() {
    // Example input
    struct Node *n1 = newNode('A', 5);
    struct Node *n2 = newNode('B', 9);
    struct Node *n3 = newNode('C', 12);
    struct Node *n4 = newNode('D', 13);
    struct Node *n5 = newNode('E', 16);
    struct Node *n6 = newNode('F', 45);

    // Build tree manually (simplified for exam)
    struct Node *n7 = newNode('$', n1->freq + n2->freq);
    n7->left = n1; n7->right = n2;

    struct Node *n8 = newNode('$', n3->freq + n4->freq);
    n8->left = n3; n8->right = n4;

    struct Node *n9 = newNode('$', n7->freq + n8->freq);
    n9->left = n7; n9->right = n8;

    struct Node *n10 = newNode('$', n5->freq + n6->freq);
    n10->left = n5; n10->right = n6;

    struct Node *root = newNode('$', n9->freq + n10->freq);
    root->left = n9; root->right = n10;

    int arr[100], top = 0;
    printCodes(root, arr, top);

    return 0;
}
